# Python
__pycache__/
*.py[cod]
*.so
python/darave.log

# Build artifacts
build/
cpp/build/

# Frontend
node_modules/
frontend/dist/
frontend/node_modules/

# Сторонние экспорты сессий — не должны попадать в репозиторий
new-session-*.json

# Логи / виртуальные окружения
*.log
.venv/
venv/

# Редакторы / ОС
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# Персистентность/записи backend'а — локальные данные, не для репозитория
darave.db
darave.db-*
recordings/
scan_dbs/
*.env
.env

# Личный конфиг автозапуска (там ключ Gemini/пути) — см.
# darave_config.example.ps1, копируется в darave_config.ps1 один раз
darave_config.ps1

# Анализ треков (стемы/кеши классификатора) — большие бинарники
stems_out/
*.wav
*.flac_ t o _ d e l e t e / 
# Папки, которые не нужно сканировать
demos/
recordings/
sets/

# На всякий случай продублируй с абсолютными путями (тоже работает)
/demos/
/recordings/
/sets/
/mixxx/skins/LateNight/

# И если там есть бинарные аудиофайлы, лучше запретить их все
*.wav
*.mp3
*.flac
*.aiff
*.m4a
 
 
