# DARAVE — ИИ-ассистент диджея для Mixxx

DARAVE отдаёт диджею голосового/текстового ассистента, который видит
телеметрию его дек (Mixxx) в реальном времени и умеет сам исполнять
переходы через виртуальный MIDI-порт — без обращения в облако на каждый
шаг микса.

Проверенная гипотеза (тактический слой): можно получить план перехода
(`MixPlan`) один раз и дальше исполнить его локально с точностью,
достаточной для реального диджеинга. На реальном `rtmidi`-порту (567
событий, BPM 128) джиттер планировщика — 0.08–0.46мс, на 1-2 порядка
меньше типичного джиттера USB-MIDI транспорта (1-10мс) — то есть узкое
место не здесь.

## Архитектура

```
companion (у диджея, рядом с Mixxx)          backend (облако, один процесс
                                               на всех арендаторов)
┌─────────────────────────────┐              ┌───────────────────────────┐
│ companion_main.py            │  WebSocket   │ server.py (FastAPI)        │
│  midi_bridge.py  -> MIDI OUT │ ───────────▶ │  session.py (комнаты)      │
│  telemetry.py    <- SysEx IN │ ◀─────────── │  agent.py (Gemini, tool-   │
│  scheduler.py    (тайминги)  │   /ws/companion/{room}   use -> MixPlan)  │
│  recording_uploader.py       │  HTTP POST   │  persistence.py (SQLite)   │
│                     запись ─┼─────────────▶│  static/chat.html          │
│  ws_client.py    (сеть)      │              └───────────┬───────────────┘
└─────────────────────────────┘                            │ /ws/chat/{room}
                                                      браузер диджея (чат +
                                                      живая телеметрия дек +
                                                      повтор микса/запись)
```

`{room}` — код комнаты (`--companion-id` у companion, поле в чате в
браузере). Один арендатор = один код = одна изолированная комната: свой
companion, своя история диалога с агентом (переживает рестарт backend'а,
см. "Персистентность"), своя телеметрия, ничего не видно другим
арендаторам. Комнаты создаются лениво и закрываются, когда из них уходят
все — но история/последний план в SQLite остаются и подхватываются заново
при следующем подключении с тем же кодом.

Веб-чат диджея (`static/chat.html`) — это не только чат: четыре вкладки
слева (Чат / Библиотека / Стратегия / Техника). Библиотека и Стратегия
читают/строят план через backend (`GET/POST /api/rooms/{room}/library`,
`POST /api/rooms/{room}/strategy`), Техника — справочник + слайдеры
параметров техник сведения (`GET /api/techniques`), напрямую из
`techniques.py`.

## Структура

- `mixplan.py` — схема плана перехода (дискретные, ramp- и hold-события —
  hold используется техниками вида "зажать reverse на N долей")
- `techniques.py` — библиотека из 22 техник сведения (DNB-00..DNB-21):
  метаданные (сложность, параметры, правило по тональности/BPM) +
  исполняемый билдер `MixPlan`-событий на каждую; см. раздел "Техники
  сведения" ниже
- `mix_strategist.py` — планировщик всего сета: порядок треков (круг
  Камелота + целевая энергодуга) + выбор техники под каждый переход +
  точка сведения по реальным дропам/брейкдаунам трека; см. раздел
  "Стратегия сета" ниже
- `llm_providers.py` — абстракция LLM-провайдера (`LLMProvider`): Gemini
  (облако) или Ollama (локально, бесплатно, без интернета); см. раздел
  "LLM-провайдеры" ниже
- `midi_mapping.py` — action → MIDI Note/CC (контракт для `.js`-мэппинга Mixxx),
  включая `MASTER_DISCRETE_NOTE`/`resolve_master_discrete` для разовых команд
  вне MixPlan (сейчас — переключение записи)
- `scheduler.py` — разворачивает план в таймированные события и исполняет
  с гибридным sleep (asyncio.sleep + busy-wait на последних ~3мс)
- `midi_bridge.py` — `MockMidiBackend` (тесты таймингов) и `RtMidiBackend` (реальный порт)
- `telemetry.py` — приём SysEx-телеметрии от Mixxx (деки + глобальный статус записи)
- `state_store.py` — потокобезопасное хранилище состояния дек + статуса записи
- `ws_client.py` — WebSocket-клиент companion → backend; шлёт телеметрию,
  принимает MixPlan и разовые команды
- `recording_uploader.py` — после остановки записи находит свежий файл в
  папке записей Mixxx и грузит его backend'у по HTTP
- `companion_main.py` — точка входа companion (главный файл запуска у диджея)
- `main.py`, `telemetry_demo.py` — более ранние демо-скрипты, можно удалить

- `server.py` — FastAPI backend: `/ws/companion/{room}`, `/ws/chat/{room}`,
  `/api/rooms` (генерация кода комнаты), `/api/rooms/{room}/recording`
  (загрузка/скачивание записи), отдаёт `static/chat.html`
- `session.py` — `SessionManager`/`SessionRoom`: мультиарендная изоляция комнат
- `persistence.py` — SQLite: история диалога, последний MixPlan и
  загруженная библиотека треков комнаты переживают рестарт backend'а
  (и — что важнее на практике — короткий обрыв связи в браузере, см.
  "Персистентность" ниже)
- `agent.py` — `DJAgent` поверх `llm_providers.LLMProvider` (Gemini или
  Ollama — не завязан на конкретный SDK): знает всю библиотеку техник из
  `techniques.py`, решает, когда и какую технику звать; сам тайминг
  перехода по-прежнему считает детерминированный код, не LLM
- `static/chat.html` — браузерный веб-интерфейс диджея, 4 вкладки: Чат
  (живая телеметрия дек + диалог с ассистентом, "повторить микс" /
  "запись" / "скачать запись"), Библиотека (отсканированные треки —
  BPM/тональность/энергия/длительность, детальная панель), Стратегия
  (энергодуга сета, порядок треков, список переходов с объяснением
  выбора техники и кнопкой "▶ выполнить"), Техника (справочник всех 22
  техник со слайдерами параметров)

- `darave-controller-scripts.js`, `DARAVE-Virtual-Controller.midi.xml` —
  JS/XML-мэппинг для Mixxx (кладётся в Mixxx settings dir, `controllers/`);
  play/cue/sync/loop/hotcue/eq/filter/crossfader/recording — декларативно
  в XML, телеметрия (деки + статус записи) — таймером в JS

- `track_analysis.py` — офлайн-сканер библиотеки: BPM/тональность (+
  перевод в код Камелота)/энергетика/структура (дропы, брейкдауны,
  границы фраз) — требует `requirements-analysis.txt`, не входит в
  основной рантайм; умеет сразу залить результат в комнату backend'а
  (`--upload`/`--room`, см. "Стратегия сета" ниже)
- `stems.py` — офлайн-разделение трека на стемы через Demucs (тоже
  `requirements-analysis.txt`)

- `Dockerfile`, `docker-compose.yml`, `Caddyfile` — деплой backend'а на VPS

- `start_darave.bat`, `start_darave.ps1`, `darave_config.example.ps1` —
  автозапуск одним кликом на Windows (loopMIDI + Mixxx + backend +
  companion + браузер), см. "Автозапуск одним кликом" ниже

## Запуск (локально, для разработки)

```bash
pip install -r requirements.txt

# backend (облако/сервер) — ключ бесплатно: https://aistudio.google.com
export GEMINI_API_KEY=...
export GEMINI_MODEL=gemini-3.6-flash  # опционально, см. agent.py
uvicorn server:app --host 0.0.0.0 --port 8765

# companion (у диджея, рядом с Mixxx; Windows — нативный Python, НЕ WSL)
python companion_main.py --midi-backend rtmidi --telemetry-backend rtmidi ^
  --ws-url ws://<адрес-backend>:8765 --companion-id my-club-fri ^
  --recordings-dir "C:\Users\<user>\Documents\Mixxx\Recordings"
```

Диджей открывает `http://<адрес-backend>:8765/?room=my-club-fri` — код
комнаты должен совпадать с `--companion-id`. `--recordings-dir` — папка
записей Mixxx (Preferences → Recordings в самом Mixxx); без неё кнопка
"скачать запись" в чате не будет работать (запись всё равно пишется
локально в Mixxx, просто не подхватится и не уйдёт в чат).

### Быстрая проверка без реального MIDI/Mixxx (mock)

```bash
python companion_main.py --midi-backend mock --telemetry-backend mock --ws-url ws://localhost:8765
```

Companion в mock-режиме сам эмулирует поток телеметрии двух дек — можно
проверить весь путь чат → агент → MixPlan → companion, не открывая Mixxx.

## Автозапуск одним кликом (Windows)

Всё из раздела выше (loopMIDI, Mixxx, backend, companion, браузер) можно
поднимать одним двойным кликом вместо ручного набора команд в нескольких
терминалах — `start_darave.bat`.

**Первый запуск:** просто двойной клик по `start_darave.bat`. Скрипт
увидит, что `darave_config.ps1` ещё нет, создаст его из шаблона и откроет
в блокноте. Заполните код комнаты, ключ Gemini (или переключите
`LlmProvider` на `"ollama"` — тогда ключ не нужен) и, если стандартные
пути не подошли, пути к `loopMIDI.exe`/`mixxx.exe`. Сохраните файл и
запустите `.bat` ещё раз. Конфиг в `.gitignore` — ключ не попадёт в архив
при обновлениях DARAVE.

**Один раз вручную** (это скрипт сделать не может, но обе программы
запоминают настройку сами): создайте порт в loopMIDI и включите/загрузите
мэппинг в Mixxx Preferences → Controllers — см. "Виртуальный MIDI-порт"
ниже.

**Дальше — просто двойной клик по `start_darave.bat`.** Скрипт запустит
loopMIDI (если ещё не запущен), Mixxx (если ещё не запущен), откроет два
окна терминала — «DARAVE backend» и «DARAVE companion» — и откроет браузер
сразу на `http://localhost:8765/?room=<ваш код>`. Чтобы остановить —
закройте эти два окна терминала (Mixxx и loopMIDI не трогает, они
отдельные программы).

**Если что-то пошло не так** — скрипт пишет всё, что делает, в
`darave_start.log` рядом с собой, показывает ошибку с номером строки и
всегда ждёт Enter перед закрытием окна (окно не может "схлопнуться"
молча). Пришлите содержимое лога — по нему видно, на каком шаге встало.

Отдельно скрипт проверяет: есть ли `python` в PATH, поднялся ли backend
(дёргает `/api/techniques` несколько раз), заполнен ли ключ Gemini — и на
каждый случай пишет понятную причину, а не просто падает.

Файлы `.ps1` сохранены в UTF-8 **с BOM** — без него Windows PowerShell 5.1
читает их как cp1251 и русский текст превращается в кракозябры.

Честно про ограничения: синтаксис проверен парсером PowerShell 7 и обе
ветки (конфига нет / конфиг есть) прогнаны живьём, но на настоящем
Windows с реальными loopMIDI/Mixxx скрипт не запускался — их нет в
песочнице.

## LLM-провайдеры: Gemini (облако) или Ollama (локально)

`agent.py` не завязан на конкретный SDK — `DJAgent` работает через
`llm_providers.LLMProvider`, и провайдер выбирается переменной окружения:

```bash
export DARAVE_LLM_PROVIDER=auto     # по умолчанию — см. ниже
export DARAVE_LLM_PROVIDER=gemini   # только облако, нужен интернет + ключ
export DARAVE_LLM_PROVIDER=ollama   # только локально, бесплатно, без интернета
```

**`auto` (по умолчанию)** выбирает сам: если задан непустой `GEMINI_API_KEY`
— Gemini; если ключа нет, но Ollama отвечает — Ollama; если нет ни того ни
другого — backend падает сразу при старте с внятным сообщением. Это
сделано после реального случая: провайдер остался `gemini` с
незаполненным ключом-плейсхолдером, backend спокойно поднялся, а каждое
сообщение в чате падало с `400 API_KEY_INVALID` — ошибка всплывала не там,
где была причина. Плейсхолдер вида `PASTE_...` теперь трактуется как
отсутствие ключа.

**Gemini** (`gemini-3.6-flash`, бесплатный тир) — проще для старта, но
недоступен из России без VPN/зарубежного хостинга (см. раздел ниже).

**Ollama** — модель крутится на своей же машине (или на сервере рядом с
backend'ом), интернет не нужен вообще, гео-блока не существует в
принципе. Нужна модель с поддержкой tool-calling — не любая локальная
LLM это умеет:

```bash
# 1. поставить Ollama: https://ollama.com/download
# 2. скачать модель с поддержкой tool-calling, например:
ollama pull llama3.1        # 8B, хороший баланс скорость/качество
# альтернативы: qwen2.5, mistral-nemo, firefunction-v2

# 3. Ollama сам поднимает http://localhost:11434 — backend найдёт его сам,
#    если нужен другой адрес/модель:
export OLLAMA_HOST=http://localhost:11434
export OLLAMA_MODEL=llama3.1

export DARAVE_LLM_PROVIDER=ollama
pip install ollama   # или requirements.txt/requirements-server.txt целиком
uvicorn server:app --host 0.0.0.0 --port 8765
```

Компромисс — качество ответов и точность выбора техники у 8B-локальной
модели заметно ниже, чем у Gemini, особенно на неоднозначных формулировках
("сделай что-нибудь позаковыристее тут"). Для чёткого запроса ("сделай
Echo Cut с деки A на деку B") разницы почти нет — обе модели просто зовут
инструмент с нужными параметрами. `_tool_schema()`/`_system_prompt()` в
`agent.py` — общие для обоих провайдеров, отдельно под Ollama ничего не
подстраивалось; если конкретная модель заметно хуже справляется с
tool-calling, попробуйте другую из списка выше или более крупный вариант
(если позволяет железо — CPU-инференс 8B терпим, 70B без GPU уже медленно).

## ⚠️ Gemini не работает из России — это geo-блок, не биллинг

Актуально, только если выбран `DARAVE_LLM_PROVIDER=gemini` (по умолчанию)
— с `ollama` этого раздела для вас не существует, локальная модель не
ходит в Google вообще. Google не пускает Gemini API и Google AI Studio из
России (а также
Беларуси, Ирана, Северной Кореи, Сирии, Кубы, материкового Китая) —
[официальный список доступных стран](https://ai.google.dev/gemini-api/docs/available-regions)
даже не упоминает эти страны. Это **блок на уровне geo/IP**, не про
деньги на счету — `gemini-3.6-flash` бесплатен (см. [pricing](https://ai.google.dev/gemini-api/docs/pricing)),
но с российского IP вызов не пройдёт вообще, с картой или без.

Два рабочих выхода:

1. **Деплоить backend на зарубежный VPS** (см. раздел "Деплой в интернет"
   ниже) — тогда вызовы к Gemini идут с IP сервера, а не диджея. Это
   решает и Gemini, и саму задачу "деплой в интернет" одним шагом — она
   всё равно нужна для сдачи сервиса в аренду.
2. **Для локальной отладки прямо сейчас** — поднять backend через
   зарубежный прокси/VPN. `google-genai` ходит через `httpx`, который сам
   уважает переменные окружения `HTTPS_PROXY`/`HTTP_PROXY`:
   ```powershell
   $env:HTTPS_PROXY="http://<ваш-прокси-или-VPN-endpoint>"
   $env:GEMINI_API_KEY="..."
   uvicorn server:app --host 0.0.0.0 --port 8765
   ```
   Если после этого чат в браузере отвечает осмысленно (не "Ошибка на
   стороне ассистента") — Gemini доехал, дело было в geo-блоке.

Отдельно: тайминг-переход (`filter_sweep`/`sync`/`crossfade`/`loop_exit`)
сверен построчно между `agent.py` → `midi_mapping.py` → `DARAVE-Virtual-Controller.midi.xml`
— номера нот/CC везде совпадают, обе деки (A и B) теперь замаплены
симметрично (в предыдущей версии XML у деки B не было `pregain`/`meta`).
Наиболее вероятная причина "переход не работает, только play/pause" —
именно то, что `build_transition_plan` ни разу не вызвался успешно из-за
падающих вызовов Gemini: агент без ответа LLM не может решить "когда"
строить план. Проверить это можно прямо сейчас в mock-режиме (пример
выше) — если после починки Gemini в чате написать "сделай переход A на
B", лог companion'а должен показать `план 'transition_...' исполнен`.

## Деплой в интернет

```bash
git clone <ваш форк/копия репозитория> && cd DARAVE
cp Caddyfile Caddyfile.local  # или просто отредактируйте домен в Caddyfile
# впишите свой домен вместо your-domain.example

echo "GEMINI_API_KEY=..." > .env
echo "GEMINI_MODEL=gemini-3.6-flash" >> .env

docker compose up -d --build
```

Требуется: любой VPS **не в списке заблокированных для Gemini стран**
(Германия/Нидерланды/Финляндия — обычно самые дешёвые европейские
варианты, ~4-6 €/мес у Hetzner/Contabo/DigitalOcean) и домен, указывающий
на его IP (A-запись) — Caddy сам получит Let's Encrypt-сертификат и
поднимет HTTPS/WSS без ручной возни. `docker-compose.yml` разворачивает
backend (в контейнере) + Caddy (реверс-прокси/TLS); `Dockerfile` собирает
только backend (`requirements-server.txt`, без `python-rtmidi` — он не
нужен на сервере, только у companion). Данные (SQLite, записи) лежат в
именованных docker-volume — переживают рестарт/обновление контейнера.

Companion по-прежнему запускается на компьютере диджея с `--ws-url
wss://ваш-домен` — MIDI/Mixxx остаются локальными, в облако уходят только
текст чата, план перехода и загруженная запись.

## Безопасность комнат

MVP-уровень: `session_id`/`--companion-id` — это и есть "пароль" на
комнату (как код встречи в Zoom), любой, кто его знает, может подключиться
к чату или встать вместо companion'а. Для сдачи в аренду:

- Не придумывайте короткие запоминающиеся коды вручную — используйте
  `POST /api/rooms` (см. `server.py`), он генерирует криптостойкий
  случайный код (`secrets.token_urlsafe(12)`, ~16 непредсказуемых
  символов) под каждого нового клиента.
- HTTPS/WSS обязателен в проде (см. "Деплой в интернет" — Caddy это
  закрывает) — иначе код комнаты и вся переписка идут открытым текстом.
- Дальнейшее усиление (не сделано, следующий шаг под настоящий биллинг):
  привязка комнаты к аккаунту/оплате, TTL на неактивные комнаты, rate
  limit на попытки подключения к `/ws/chat/{id}` и `/ws/companion/{id}`.

## Персистентность

История диалога DJAgent и последний отправленный MixPlan хранятся в SQLite
(`persistence.py`, файл `darave.db` рядом с `server.py`, путь настраивается
через `DARAVE_DB_PATH`) и переживают рестарт backend'а: при следующем
подключении с тем же кодом комнаты агент видит прежнюю историю (обрезанную
до последних 40 реплик), а кнопка "повторить микс" работает даже после
рестарта backend'а между сетами. Живая телеметрия (BPM/позиция дек) не
персистится намеренно — она всё равно устаревает за секунды и
переотправляется companion'ом заново при подключении.

## Проиграть микс заново / запись / скачать запись

В чате (`static/chat.html`) под логом сообщений — три элемента управления:

- **🔁 Повторить микс** — переотправляет companion'у последний MixPlan
  этой комнаты (хранится в `SessionRoom.last_plan`, переживает рестарт
  backend'а через `persistence.py`).
- **⏺ Запись** — шлёт companion'у команду `recording_toggle`
  (`MASTER_DISCRETE_NOTE` в `midi_mapping.py` → `[Recording]
  toggle_recording` в XML). Кнопка сама показывает актуальный статус —
  companion шлёт статус записи Mixxx (`[Recording],status`) тем же путём,
  что и телеметрию дек.
- **⬇ Скачать запись** — появляется, когда companion сам обнаружит, что
  запись остановилась (`recording_upload_watcher` в `companion_main.py`
  следит за статусом, не важно, остановлена запись кнопкой в чате или
  прямо в Mixxx), найдёт свежий файл в `--recordings-dir` и загрузит его
  backend'у (`recording_uploader.py` → `POST /api/rooms/{room}/recording`).
  Скачивание — `GET /api/rooms/{room}/recording`, файл хранится на
  backend'е до следующей записи (перезаписывается).

## Анализ треков: BPM, тональность, энергетика, стемы

Отдельно от рантайма (companion/backend работают в реальном времени, тяжёлые
ML-зависимости туда не тащим) — `track_analysis.py` и `stems.py`,
зависимости в `requirements-analysis.txt`:

**Из веб-интерфейса** (вкладка "Библиотека", кнопка "🔍 Сканировать") —
самый простой способ: впишите путь к папке с музыкой в поле рядом с
кнопкой и нажмите её. Backend сам запустит `track_analysis.py` подпроцессом
и сразу загрузит результат в текущую комнату — прогресс сканирования
(файл за файлом) виден прямо под кнопкой, библиотека обновляется
автоматически по готовности. **Условие**: работает, только если backend и
музыка на одной машине (обычный локальный запуск рядом с Mixxx, как в
разделе "Автозапуск" выше) и в его Python-окружении установлен
`requirements-analysis.txt` — если сканировать из веб-UI при облачном
деплое backend'а на VPS, кнопка не найдёт вашу музыку (она на VPS не
видна) — там по-прежнему нужен способ ниже.

**Из терминала** (работает всегда, в т.ч. при облачном backend'е — грузит
результат по HTTP на его адрес):

```bash
pip install -r requirements-analysis.txt
python track_analysis.py --scan "C:\Music" --db library.db
```

Считает по каждому треку: BPM (`librosa.beat.beat_track`, с фолбэком на
`onset_strength`-темп, если основной трекер долей не справился — тихие
интро/нестандартный ритм), тональность (хрома-вектор + корреляция с
профилями Крумхансла-Шмуклера — 24 варианта, мажор/минор × 12 тоник),
энергетика (RMS), "яркость" (спектральный центроид) и грубая
танцевальность (регулярность межбитовых интервалов). Пишет в SQLite
(`library.db`, таблица `tracks`).

```bash
pip install -r requirements-stems.txt
python stems.py --track "C:\Music\track.mp3" --out-dir stems_out
```

Разделяет трек на 4 стема (vocals/drums/bass/other) через предобученную
модель Demucs (htdemucs). Это **офлайн**-препроцессинг одного трека
заранее, не живое независимое управление 4 стемами на деке во время сета.

## Техники сведения (22, вкладка "Техника" в чате)

`techniques.py` — библиотека техник сведения, каждая с метаданными
(сложность 1-5, максимально допустимая разница BPM, требование к
тональности, направление энергии, нужны ли стемы/сколько дек) и реально
исполняемым билдером `MixPlan`-событий — не просто описание для LLM, а
рабочий код, прогнанный через `PlanScheduler` на каждую технику:

Long Blend, Quick Cut, Harmonic Blend, Bass Swap, Filter Sweep, Backspin
Transition, Echo Cut, EQ Roller, Loop & Roll, Reverse Drop, Key Jump,
Phrase Match, Fader FX Series, Delay Out, Triple Drop (нужно 3+ деки),
Double Drop, Mashup Transition, Beatmatch Blend, Tension Riser, Bass Cut
Swap, Drop Swap, A Cappella Overlay.

**Честно про A Cappella Overlay** — единственная из 22, которая не
собирает план: реально нужен изолированный вокальный стем в реальном
времени поверх второй деки, а `stems.py` пока умеет только офлайн-
разделение целого трека заранее (см. "Что ещё не сделано" ниже), не живое
наложение стема во время сета. Вызов технически определён (описание,
параметры — модель может её "выбрать"), но `build_plan()` кидает понятное
`NotImplementedError`, а не тихо строит план, который не сработает.

Диджей может выбрать технику и руками (вкладка "Техника" — слайдеры
параметров, напр. для Echo Cut: длительность эха, громкость эха, точка
включения, скорость открытия mid), и через чат текстом ("сделай Echo Cut
с A на B") — агент сам вызывает нужный `technique_id`, если формулировка
явно на него указывает (см. `_system_prompt()` в `agent.py`), либо
Long Blend по умолчанию.

## Стратегия сета — автоматическое планирование микса (вкладка "Стратегия")

`mix_strategist.py` — **не ML-модель**, объяснимый rule-engine поверх
результатов `track_analysis.py`. По загруженной библиотеке комнаты
строит:

1. **Порядок треков** — жадный алгоритм: на каждом шаге берёт из
   оставшихся трек, максимизирующий `0.6 × гармоническая_совместимость` (по
   кругу Камелота с предыдущим треком) `+ 0.4 × близость к целевой энергии`
   этого места в сете. Целевая энергодуга — три пресета: "rising" (плавный
   рост EL1→EL5 с передышкой перед финалом, как на референсе), "wave",
   "peak_middle".
2. **Технику под каждый переход** — приоритетные правила (в самом выводе
   есть `rule`: человекочитаемое объяснение, почему выбрана именно эта
   техника, и `alternatives` — что ещё подходило): несовместимые типы BPM
   → Quick Cut; конфликт тональностей → Echo Cut маскирует; рост энергии и
   у обоих треков есть дроп → Double Drop; рост энергии без дропа →
   Tension Riser нагнетает; почти идентичные тональности → длинный
   Harmonic Blend; иначе — Long Blend по умолчанию.
3. **Точку сведения внутри трека** — не "просто через 32 такта", а
   реальный дроп/брейкдаун из `track_analysis.py::detect_structure()`
   (эвристика по сглаженному RMS: устойчивый скачок вверх = дроп,
   устойчивый провал = брейкдаун/яма), либо ближайшая граница фразы (сетка
   каждые 16 тактов), если дропов не нашлось.

Как этим пользоваться:

```bash
# 1. отсканировать библиотеку И сразу залить в комнату backend'а:
python track_analysis.py --scan "C:\Music" --db library.db \
  --upload http://<адрес-backend>:8765 --room my-club-fri

# 2. во вкладке "Стратегия" в браузере — выбрать форму энергодуги,
#    нажать "Построить план": список переходов с объяснением и кнопкой
#    "▶ выполнить переход N" — она материализует технику в реальный
#    MixPlan и шлёт его companion'у, как обычный чат-запрос.
```

Загруженная библиотека комнаты сохраняется в SQLite (`persistence.py`) —
переживает не только рестарт backend'а, но и обычный обрыв связи в
браузере (комната в памяти удаляется, когда из неё уходят все, и раньше
это тихо стирало библиотеку — теперь она подхватывается заново из БД при
следующем подключении с тем же кодом комнаты).

### Что ещё не сделано (жанр/стиль/инструменты, живые стемы)

- **Жанр/стиль/"какие инструменты в треке"** — в отличие от BPM/тональности,
  это не считается сигнальной обработкой напрямую, нужна предобученная
  модель аудио-тегирования (например, PANNs/YAMNet поверх AudioSet —
  выдаёт теги вроде "Guitar", "Drum kit", "Singing", а не просто жанр
  одним словом). `track_analysis.py::classify_genre_instruments()` — уже
  готовая точка расширения: если `panns_inference` установлен, вызывает
  его и возвращает теги с уверенностью; если нет — честно возвращает
  `None`, а не выдумывает жанр. Следующий шаг — оценить конкретную модель
  (веса ~300-500 МБ, CPU-инференс на трек — секунды, не минуты) и решить,
  насколько точность отвечает ожиданиям, прежде чем встраивать в пайплайн
  сканирования по умолчанию.
- **Живое независимое управление 4 стемами на деке** ("Traktor-style") —
  Mixxx не поддерживает мультистем нативно. Рабочая гипотеза не
  проверялась: эмулировать через 4 скрытых синхронизированных канала +
  companion веером рассылает команды на все 4. Главный риск — sample-drift
  между каналами на длинных треках, нужен отдельный эксперимент на
  синхронизацию, прежде чем строить сам механизм. `stems.py` пока даёт
  только офлайн-разделение — готовые файлы можно руками закинуть на
  отдельную деку в Mixxx уже сегодня, без ожидания живой версии.

## Виртуальный MIDI-порт

**Linux/macOS** — `rtmidi` создаёт virtual port сам, ничего ставить не нужно.
Проверить появление порта: `aconnect -l` (Linux) / Audio MIDI Setup (macOS).

**Windows** — у RtMidi нет API для virtual-портов, нужен посредник:
1. Поставить [loopMIDI](https://www.tobias-erichsen.de/software/loopmidi.html)
2. Создать в нём порт с именем `DARAVE Virtual Controller`
   (или другим — передать тем же именем в `--port-name`)
3. Запустить companion — он найдёт порт по имени и откроет его

Дальше в Mixxx: Preferences → Controllers → включить `DARAVE Virtual
Controller`, загрузить `DARAVE-Virtual-Controller.midi.xml` /
`darave-controller-scripts.js`. `--controllerDebug` при запуске Mixxx —
лучший способ проверить, что реально доходит и резолвится в контролы.

## Известные грабли

- Никогда не вставляйте `GEMINI_API_KEY`/`ANTHROPIC_API_KEY` в чат,
  issue, коммит и т.п. — только в переменную окружения на своей машине.
  Если ключ всё же засветился где-то — считайте его скомпрометированным и
  сразу отзовите в консоли провайдера, даже если на счету пусто.
  **Это уже произошло дважды с текущим Gemini-ключом** (засветился в
  вставленных PowerShell-транскриптах через `$env:GEMINI_API_KEY=...`) —
  отзовите его в [Google AI Studio](https://aistudio.google.com) (Get API
  key → удалить/отозвать) и создайте новый, прежде чем деплоить в прод.
- **`python-rtmidi` на Windows есть только для Python 3.8–3.12.** Проверено
  по PyPI: у последней версии 1.5.8 колёса `win_amd64` собраны для
  cp38–cp312, для 3.13 и 3.14 их нет ни в одном релизе — сборка из
  исходников потребует MSVC. Companion (ему нужен rtmidi) запускайте
  Python 3.12. Backend можно и на новее — ему rtmidi не нужен.
  `start_darave.ps1` это проверяет сам: если в основном Python нет
  rtmidi, он пробует найти 3.12 через лаунчер `py -3.12`, а если не
  находит — поднимает companion в mock-режиме и пишет, что делать,
  вместо падения с `ModuleNotFoundError`.
- Виртуальный MIDI-порт на Windows требует loopMIDI (RtMidi сам
  virtual-порт на Windows не создаёт, только на Linux/macOS).
- WSL не видит Windows MIDI-подсистему — companion с `--midi-backend rtmidi`
  нужно запускать нативным Windows Python, не из WSL-шелла.
- Gemini API/AI Studio недоступны из России — см. раздел выше, это не
  временный сбой и не про баланс счёта.

## Что дальше

1. Довести "жанр/стиль/инструменты" до продакшна — выбрать и подключить
   конкретную модель аудио-тегирования (см. раздел "Анализ треков" выше),
   встроить результат как контекст для DJAgent (сейчас `track_analysis.py`
   не связан с `agent.py` — агент не знает о содержимом библиотеки).
2. Живые 4-стема на деке — сначала эксперимент на sample-drift между
   синхронизированными каналами, потом сам механизм рассылки на 4 канала.
3. Более строгая изоляция арендаторов — привязка комнаты к аккаунту/оплате,
   TTL неактивных комнат, rate limit на подключения (см. "Безопасность
   комнат" — генерация случайных кодов уже есть, это следующий слой).
4. Провайдер LLM — теперь два варианта, Gemini (облако, недоступен из РФ
   без VPN, см. раздел выше) и Ollama (локально, без гео-блока, но слабее
   на неоднозначных формулировках — см. "LLM-провайдеры"). Если заведёте
   оплаченный аккаунт Anthropic — вся tool-use логика в `agent.py` уже
   отделена через `LLMProvider`, добавить третий провайдер (Claude) —
   это новый класс в `llm_providers.py`, без изменений в `agent.py`.
5. `mix_strategist.py` — эвристика, не ML: не учитывает жанр/стиль (пока
   не решён пункт про аудио-тегирование выше) и не "слушает" фактическое
   звучание перехода — приоритетные правила подобраны вручную по здравому
   смыслу диджеинга, не обучены на реальных сетах. Следующий шаг для
   повышения качества — накопить фидбек диджея (принял/отклонил
   предложенный переход) и хотя бы пересчитывать веса правил по нему,
   не обязательно сразу полноценный ML.
