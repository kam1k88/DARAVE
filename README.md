# DARAVE

ИИ-диджей-ассистент. Вы пишете в чат — он сводит треки в Mixxx.

DARAVE слушает, что играет на деках, знает вашу библиотеку, выбирает технику сведения и отправляет MIDI-команды в Mixxx: крутит фильтры, эквалайзер, кроссфейдер и эффекты, ровно по битам.

Работает с Gemini (облако) или Ollama (локально, бесплатно).

## Запуск

1. Скопируйте `darave_config.example.ps1` в `darave_config.ps1`, впишите ключ Gemini или настройте Ollama
2. Запустите `start_darave.bat`
3. Откройте `http://localhost:8765/?room=my-room`

## Из чего состоит

| Что | Файл | Зачем |
|-----|------|-------|
| Backend | `server.py` | Чат, библиотека, стратегия, экспорт |
| Агент | `agent.py` | LLM решает, какой техникой сводить |
| Техники | `techniques.py` | 20+ техник сведения, детерминированный MIDI |
| Стратег | `mix_strategist.py` | Собирает сет из всей библиотеки |
| Компаньон | `companion_main.py` | Сидит рядом с Mixxx, шлёт MIDI |
| Анализ | `track_analysis.py` | BPM, тональность, структура (librosa) |
| Чат-интерфейс | `static/chat.html` | Веб-интерфейс: чат + библиотека + план |

## Провайдеры LLM

| Провайдер | Как |
|-----------|-----|
| `gemini` | Ключ с aistudio.google.com. Не работает из РФ без прокси. |
| `ollama` | Локально, бесплатно, без интернета. `ollama pull llama3.1` |
| `auto` | Gemini если есть ключ, иначе Ollama |

## Деплой на VPS

```
docker compose up -d --build
```

Backend поднимается с Caddy (HTTPS). Companion всегда живёт на машине диджея рядом с Mixxx.

## Безопасность

Код комнаты — криптостойкий токен. Чужой companion не проникнет в вашу комнату.
