# Отдельный список — это ML-зависимости для офлайн-анализа библиотеки
# (BPM/тональность/энергетика) И для аудио-демо техник (demo_render.py —
# кнопка "🎧 Демо" во вкладке "Техника"). Не нужны ни companion'у в
# реальном времени, ни backend'у для чата. Нужны для кнопки "Сканировать"
# и кнопки "Демо" (backend запускает их тем же Python, что и сам backend)
# либо для ручного запуска track_analysis.py из терминала. Ставить:
#   pip install -r requirements-analysis.txt
librosa>=0.10.2
soundfile>=0.12.1
numpy>=1.26.0
scipy>=1.11.0  # уже тянется librosa как зависимость — здесь явно для demo_render.py

# Разделение трека на стемы (vocals/drums/bass/other) — отдельная, более
# тяжёлая возможность (тянет torch, ~2-3 ГБ) и НЕ входит сюда. Нужна,
# только если хотите пользоваться stems.py — см. requirements-stems.txt.

# Кодирование MP3 320 кбит/с. libsndfile (через soundfile) упирается в
# ~128 кбит/с и по умолчанию отдаёт ~74 — для экспорта сета этого мало.
# lameenc — чистый pip-пакет, колёса под Windows/Linux, без ffmpeg.
lameenc>=1.7.0

# Теги файла (жанр, исполнитель) для genre.py. Без mutagen жанр
# определяется только по имени файла и темпу — работает, но теряет ту
# треть библиотеки, где жанр записан в ID3 и больше нигде.
mutagen>=1.47.0
